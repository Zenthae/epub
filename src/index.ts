export {};

// load() => get raw data and transform it to a zip archive

// parse() => get zip archive and return an EBook object populated with the correspondig data

// serialize() => transform an EBook object into a zip archive
